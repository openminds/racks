// alert("<%= escape_javascript(render :partial => 'new', :locals => {:device => @device, :interfaces => @interfaces}) %>");
$("#modal_form").html("<%= escape_javascript(render :partial => 'new', :locals => {:device => @device, :interfaces => @interfaces}) %>");
$("#modal_form_for_new_device").dialog({
	modal: true,
	close: function(event, ui) { 
		$("#new_device > div.tabs ").tabs("destroy");
		$("#new_device > div.tabs ").html(" ");
		$("#modal_form_for_new_device").dialog("destroy");
		$("#modal_form").html("  ");
	 },
	width:900
	});
$('#new_device > div.tabs ').tabs();