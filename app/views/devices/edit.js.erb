// alert("<%= escape_javascript(render :partial => 'new', :locals => {:device => @device, :interfaces => @interfaces}) %>");
$("#modal_form").html("<%= escape_javascript(render :partial => 'edit', :locals => {:device => @device, :interfaces => @interfaces}) %>");
$("#modal_form_for_device<%= @device.id %>").dialog({
	modal: true,
	close: function(event, ui) { 
		$("#edit_device_<%= @device.id %> > div.tabs ").tabs("destroy");
		$("#edit_device_<%= @device.id %> > div.tabs ").html(" ");
		$("#modal_form_for_device<%= @device.id %>").dialog("destroy");
		$("#modal_form").html("  ");
	 },
	width:900
	});
$('#edit_device_<%= @device.id %> > div.tabs ').tabs();