$("#modal-form").load("<%= new_datacenter_server_rack_path %>", function(){
	$("#modal_form_for_new_rack").dialog({
		modal: true,
		close: function(event, ui) { 
			$("#modal_form_for_new_rack").dialog("destroy");
			$("#modal-form").empty(); 
		},
		width:400
		});
	// $("#modal_form_for_new_rack").dialog("open");
});
